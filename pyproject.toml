[project]
name = "galgame-plugin"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = []

[tool.neko.build]
exclude_dirs = ["tests", "training"]
exclude_files = ["galgame_store.json", "galgame_store.json.lock", "test_ocr_pipeline.py"]

[tool.neko.pack]
exclude_dirs = ["tests", "training"]
exclude_files = ["galgame_store.json", "galgame_store.json.lock", "test_ocr_pipeline.py"]
