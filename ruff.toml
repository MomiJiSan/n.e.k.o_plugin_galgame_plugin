# Generated by neko-plugin
# managed-template: plugin-market-actions-v1
target-version = "py311"
line-length = 120
extend-exclude = ["vendor"]
respect-gitignore = false

[lint]
select = ["E4", "E7", "E9", "F", "I"]

[lint.isort]
# The reusable market workflow lints this repository as a child directory, so
# these injected/runtime namespaces are classified as third-party there. Keep
# root-local linting aligned with that canonical CI invocation.
known-third-party = ["market_plugins", "plugin", "tests"]
